import Foundation
import Domain

/*public final class MainMapper {
    static func toGetPokemons(viewModel: MainViewModel) -> Pokemons {
        return Pokemons(count: viewModel.count, next: viewModel.next!, previous: viewModel.previous!, results: viewModel.results)
    }
}
*/
